services:
  - type: web
    name: proven-backend
    runtime: node
    plan: free # or your preferred plan
    region: frankfurt # update to your preferred region
    buildCommand: npm install --include=dev && npx prisma generate && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NPM_CONFIG_PRODUCTION
        value: false
      - key: DATABASE_URL
        fromDatabase:
          name: proven-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: proven-redis
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: RESEND_API_KEY
        sync: false
      - key: EMAIL_FROM
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false
      - key: FRONTEND_URL
        value: "https://www.provenworld.com"
      - key: PORT
        value: 5000

  - type: redis
    name: proven-redis
    plan: free # or your preferred plan
    region: frankfurt
    ipAllowList: [] # Allow all internal traffic
