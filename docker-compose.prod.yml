# 🚀 ProVen Production Orchestration
version: '3.8'

services:
  backend:
    build: .
    container_name: proven-backend
    restart: always
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=5000
    ports:
      - "5000:5000"
